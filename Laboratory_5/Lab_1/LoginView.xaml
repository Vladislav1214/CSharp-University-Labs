<Window x:Class="Lab_1.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab_1"
        mc:Ignorable="d"
        Title="Login" Height="800" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="Transparent">

    <Grid>
        <Border>
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FFC300" Offset="0.0" />
                    <GradientStop Color="#FF5733" Offset="0.25" />
                    <GradientStop Color="#C70039" Offset="0.5" />
                    <GradientStop Color="#900C3F" Offset="0.75" />
                    <GradientStop Color="#581845" Offset="1.0" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <Border Background="White" Margin="5">
            <StackPanel VerticalAlignment="Center" Margin="40">

                <TextBlock Text="Login" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <TextBlock Text="Username" Foreground="Gray" Margin="0,0,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="Gray"/>
                    <TextBox Grid.Column="1" x:Name="UsernameTextBox" Tag="Type your username" Style="{StaticResource TextBoxWithPlaceholder}"/>
                </Grid>

                <TextBlock Text="Password" Foreground="Gray" Margin="0,20,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="&#xE72E;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="Gray"/>
                    <PasswordBox Grid.Column="1" x:Name="PasswordBox" Tag="Type your password" Style="{StaticResource PasswordBoxWithPlaceholder}"/>
                </Grid>

                <TextBlock Text="Forgot password?" HorizontalAlignment="Right" Foreground="Gray" Margin="0,10,0,30">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="TextDecorations" Value="Underline"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Button Content="LOGIN" Click="LoginButton_Click" Foreground="White" FontSize="18" FontWeight="Bold" Height="50" Cursor="Hand">
                    <Button.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#00FFFF" Offset="0.0" />
                            <GradientStop Color="#FF00FF" Offset="1.0" />
                        </LinearGradientBrush>
                    </Button.Background>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="25">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <TextBlock Text="Or Sign Up Using" HorizontalAlignment="Center" Foreground="Gray" Margin="0,30,0,15"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Background="Transparent" BorderThickness="0" Cursor="Hand" Click="GoogleLogin_Click">
                        <TextBlock Text="G" FontSize="30" Margin="10" Foreground="#1DA1F2" Cursor="Hand"/>
                    </Button>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,40,0,0">
                    <TextBlock Text="Have not account yet? " Foreground="Gray"/>
                    <TextBlock Text="SIGN UP" MouseLeftButtonDown="SignUp_MouseLeftButtonDown" FontWeight="Bold" Foreground="Black" Cursor="Hand">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="TextDecorations" Value="Underline"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</Window>
