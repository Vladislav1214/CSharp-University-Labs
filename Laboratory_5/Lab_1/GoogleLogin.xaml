<Window x:Class="Lab_1.GoogleLogin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab_1"
        mc:Ignorable="d"
        Title="Sign in - Google Accounts" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">

    <StackPanel Margin="40">
        <TextBlock Text="G o o g l e" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,10,0,30" Foreground="#4285F4"/>

        <TextBlock Text="Sign in" FontSize="22" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Text="Email" Style="{StaticResource LabelStyle}"/>
        <TextBox x:Name="EmailTextBox"/>
        <TextBlock x:Name="EmailErrorText" Text="Email cannot be empty" Style="{StaticResource ErrorTextStyle}"/>

        <TextBlock Text="Password" Style="{StaticResource LabelStyle}"/>
        <PasswordBox x:Name="PasswordBox"/>
        <TextBlock x:Name="PasswordErrorText" Text="Password cannot be empty" Style="{StaticResource ErrorTextStyle}"/>

        <Button x:Name="LoginButton" Content="Next" Click="LoginButton_Click" 
                Width="100" HorizontalAlignment="Right" Margin="0,20,0,0"
                Background="#1A73E8" Foreground="White" FontWeight="Bold" Height="36">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </StackPanel>
</Window>