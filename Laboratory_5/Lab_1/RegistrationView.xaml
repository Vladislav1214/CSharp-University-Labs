<Window x:Class="Lab_1.RegistrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab_1"
        mc:Ignorable="d"
        Title="Registration Form" Height="600" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#4FB2E3">
            <TextBlock Text="Registration Form" Foreground="White" FontSize="20" 
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>

        <StackPanel Grid.Row="1" Margin="40,20,40,20">

            <TextBlock Text="Username" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="UsernameTextBox" LostFocus="TextBox_LostFocus" ToolTip="Type here"/>
            <TextBlock x:Name="UsernameErrorText" Style="{StaticResource ErrorTextStyle}"/>

            <TextBlock Text="Email" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="EmailTextBox" LostFocus="TextBox_LostFocus" ToolTip="johnsmith@mail.com"/>
            <TextBlock x:Name="EmailErrorText" Style="{StaticResource ErrorTextStyle}"/>

            <TextBlock Text="Password" Style="{StaticResource LabelStyle}"/>
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged"/>
            <TextBlock x:Name="PasswordErrorText" Style="{StaticResource ErrorTextStyle}"/>

            <TextBlock Text="Confirm password" Style="{StaticResource LabelStyle}"/>
            <PasswordBox x:Name="ConfirmPasswordBox" PasswordChanged="PasswordBox_PasswordChanged"/>
            <TextBlock x:Name="ConfirmPasswordErrorText" Style="{StaticResource ErrorTextStyle}"/>

            <Button x:Name="FinalRegisterButton" Content="REGISTER" Height="45" FontSize="16" FontWeight="SemiBold" 
                    Foreground="White" Background="#E86C54" Margin="0,40,0,20" Click="FinalRegisterButton_Click"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <TextBlock Text="Already have an account? " Foreground="#B0B0B0"/>
                <TextBlock Text="Login" FontWeight="Bold" Foreground="#4FB2E3" Cursor="Hand" MouseLeftButtonDown="Login_Click"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>