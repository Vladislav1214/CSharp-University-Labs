<Window x:Class="Lab_1.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="600" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="Transparent">

    <Grid>
        <Border>
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FFC300" Offset="0.0" />
                    <GradientStop Color="#FF5733" Offset="0.25" />
                    <GradientStop Color="#C70039" Offset="0.5" />
                    <GradientStop Color="#900C3F" Offset="0.75" />
                    <GradientStop Color="#581845" Offset="1.0" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <Border Background="White" Margin="10" CornerRadius="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center" Margin="30">
                <TextBlock Text="Login" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <TextBlock Text="Username" Style="{StaticResource LabelStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" 
                               FontSize="20" VerticalAlignment="Center" Margin="0,0,10,20" Foreground="Gray"/>
                    <TextBox Grid.Column="1" x:Name="UsernameTextBox"/>
                </Grid>

                <TextBlock Text="Password" Style="{StaticResource LabelStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="&#xE72E;" FontFamily="Segoe MDL2 Assets" 
                               FontSize="20" VerticalAlignment="Center" Margin="0,0,10,20" Foreground="Gray"/>
                    <PasswordBox Grid.Column="1" x:Name="PasswordBox"/>
                </Grid>

                <TextBlock Text="Forgot password?" HorizontalAlignment="Right" Style="{StaticResource LinkTextStyle}" 
                           FontSize="12" Foreground="Gray" FontWeight="Normal" Margin="0,-10,0,30"/>

                <Button Content="LOGIN" Click="LoginButton_Click" Style="{StaticResource RoundedButtonStyle}">
                    <Button.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#00FFFF" Offset="0.0" />
                            <GradientStop Color="#FF00FF" Offset="1.0" />
                        </LinearGradientBrush>
                    </Button.Background>
                </Button>

                <TextBlock Text="Or Sign Up Using" HorizontalAlignment="Center" Foreground="Gray" Margin="0,30,0,15"/>

                <Button Background="Transparent" BorderThickness="0" Cursor="Hand" Click="GoogleLogin_Click" HorizontalAlignment="Center">
                    <TextBlock Text="G" FontSize="30" FontWeight="Bold" Foreground="#1DA1F2"/>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                    <TextBlock Text="Don't have an account? " Foreground="Gray"/>
                    <TextBlock Text="SIGN UP" MouseLeftButtonDown="SignUp_MouseLeftButtonDown" Style="{StaticResource LinkTextStyle}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>